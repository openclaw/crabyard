FROM golang:1.25-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY cmd ./cmd
RUN CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /out/crabyard-ssh-gateway ./cmd/crabyard-ssh-gateway

FROM alpine:3.22
RUN adduser -D -H -s /sbin/nologin crabyard
USER crabyard
COPY --from=build /out/crabyard-ssh-gateway /usr/local/bin/crabyard-ssh-gateway
EXPOSE 2222
ENTRYPOINT ["/usr/local/bin/crabyard-ssh-gateway"]
